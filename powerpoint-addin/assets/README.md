# Add-in icon assets

`icon-16.png`, `icon-32.png`, and `icon-80.png` in this folder are minimal
1x1 placeholder PNGs so the manifest's icon URLs resolve to real files out
of the box. **Replace them with properly-sized, branded icons** (16x16,
32x32, 80x80 px) before publishing the add-in.
